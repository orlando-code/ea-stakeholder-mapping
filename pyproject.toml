[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ea-stakeholder-mapping"
version = "0.1.0"
description = "EA Stakeholder Mapping package"
requires-python = ">=3.7"
dependencies = [
    # Add your dependencies here, for example:
    # "pandas",
    # "numpy",
]

[tool.setuptools.packages.find]
where = ["."]

